{
  "name": "beignet-ts",
  "private": true,
  "type": "module",
  "description": "Beignet TypeScript head, pull worker, Pi executor, and protocol conformance",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "executor": "node executor/executor.ts",
    "worker": "node executor/worker.ts",
    "fakecar": "node executor/fakecar.ts",
    "head": "node head/head.ts",
    "test": "node --test executor/test/*.test.ts",
    "conformance": "node executor/conformance.ts",
    "docs:dev": "vitepress dev docs",
    "docs:build": "vitepress build docs"
  },
  "dependencies": {
    "@earendil-works/pi-ai": "0.85.1",
    "@earendil-works/pi-coding-agent": "0.85.1"
  },
  "devDependencies": {
    "vitepress": "1.6.4"
  }
}
